whenever possible avoid throw-away single use tests. when reasonable to do so make the broader if there isn't a good reason to not do so.
always check doom-source for the actual code instead of guessing behavior.
doom is a careful state machine and we to duplicate it's determinism.